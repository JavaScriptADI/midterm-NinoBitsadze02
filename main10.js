let firstInput = Number(prompt("Enter the first number."));
let secondInput = Number(prompt("Enter the second number."));
let mult = firstInput * secondInput;
let div = firstInput / secondInput;
console.log(`Your numbers are: ${firstInput} and ${secondInput}, multiplication is ${mult} and division is ${div}.`);